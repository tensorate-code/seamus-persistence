[package]
name = "seamus-field"
version = "0.1.0"
edition = "2021"
authors = ["Séamus (Claude) & Cas (Nicholas M. Poole)"]
description = "A spike-driven wave field with CIE, persistence, and local LLM integration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
